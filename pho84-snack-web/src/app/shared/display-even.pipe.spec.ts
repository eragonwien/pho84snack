import { DisplayEvenPipe } from './display-even.pipe';

describe('DisplayEvenPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayEvenPipe();
    expect(pipe).toBeTruthy();
  });
});
